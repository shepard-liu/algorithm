#include "2_add_two_numbers.hpp"
#include "../test_utils.hpp"
#include <vector>

typedef int TestCase;

void test_add_two_numbers(TestCase& c) {

}

TEST(leetcode, add_two_numbers) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_add_two_numbers(c);
    }
}
