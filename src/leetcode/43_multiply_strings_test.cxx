#include "43_multiply_strings.hpp"
#include "../test_utils.hpp"
#include <vector>

typedef int TestCase;

void test_multiply_strings(TestCase& c) {

}

TEST(leetcode, multiply_strings) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_multiply_strings(c);
    }
}
