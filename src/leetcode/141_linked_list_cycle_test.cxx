#include "141_linked_list_cycle.hpp"
#include "../test_utils.hpp"
#include <vector>

typedef int TestCase;

void test_linked_list_cycle(TestCase& c) {

}

TEST(leetcode, linked_list_cycle) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_linked_list_cycle(c);
    }
}
