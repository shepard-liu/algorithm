#include "228_summary_ranges.hpp"
#include "../test_utils.hpp"
#include <vector>

typedef int TestCase;

void test_summary_ranges(TestCase& c) {

}

TEST(leetcode, summary_ranges) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_summary_ranges(c);
    }
}
