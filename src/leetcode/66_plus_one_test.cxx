#include "66_plus_one.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_plus_one(TestCase& c) {

}

TEST(leetcode, plus_one) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_plus_one(c);
    }
}

