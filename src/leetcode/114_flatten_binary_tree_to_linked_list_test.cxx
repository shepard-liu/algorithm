#include "114_flatten_binary_tree_to_linked_list.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_flatten_binary_tree_to_linked_list(TestCase& c) {

}

TEST(leetcode, flatten_binary_tree_to_linked_list) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_flatten_binary_tree_to_linked_list(c);
    }
}

