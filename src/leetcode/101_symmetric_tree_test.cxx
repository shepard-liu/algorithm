#include "101_symmetric_tree.hpp"

#include <vector>

#include "../test_utils.hpp"

typedef int TestCase;

void test_symmetric_tree(TestCase& c) {}

TEST(leetcode, symmetric_tree) {
    std::vector<TestCase> cases{};

    for (TestCase& c : cases) {
        test_symmetric_tree(c);
    }
}
