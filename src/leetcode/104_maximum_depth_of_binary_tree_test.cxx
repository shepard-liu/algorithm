#include "104_maximum_depth_of_binary_tree.hpp"

#include <vector>

#include "../test_utils.hpp"

typedef int TestCase;

void test_maximum_depth_of_binary_tree(TestCase& c) {}

TEST(leetcode, maximum_depth_of_binary_tree) {
    std::vector<TestCase> cases{};

    for (TestCase& c : cases) {
        test_maximum_depth_of_binary_tree(c);
    }
}
