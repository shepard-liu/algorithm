#include "23_merge_k_sorted_lists.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_merge_k_sorted_lists(TestCase& c) {

}

TEST(leetcode, merge_k_sorted_lists) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_merge_k_sorted_lists(c);
    }
}

