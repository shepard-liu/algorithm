#include "637_average_of_levels_in_binary_tree.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_average_of_levels_in_binary_tree(TestCase& c) {

}

TEST(leetcode, average_of_levels_in_binary_tree) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_average_of_levels_in_binary_tree(c);
    }
}

