#include "9_palindrome_number.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_palindrome_number(TestCase& c) {

}

TEST(leetcode, palindrome_number) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_palindrome_number(c);
    }
}

