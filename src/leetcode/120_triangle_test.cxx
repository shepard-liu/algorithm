#include "120_triangle.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_triangle(TestCase& c) {

}

TEST(leetcode, triangle) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_triangle(c);
    }
}

