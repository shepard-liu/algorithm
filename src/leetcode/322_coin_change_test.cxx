#include "322_coin_change.hpp"

#include <utility>
#include <vector>

#include "../test_utils.hpp"
typedef int TestCase;

void test_coin_change(TestCase& c) {}

TEST(leetcode, coin_change) {
    std::vector<TestCase> cases{};

    for (TestCase& c : cases) {
        test_coin_change(c);
    }
}
