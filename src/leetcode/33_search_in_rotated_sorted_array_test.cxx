#include "33_search_in_rotated_sorted_array.hpp"
#include "../test_utils.hpp"
#include <vector>
typedef int TestCase;

void test_search_in_rotated_sorted_array(TestCase& c) {

}

TEST(leetcode, search_in_rotated_sorted_array) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_search_in_rotated_sorted_array(c);
    }
}

