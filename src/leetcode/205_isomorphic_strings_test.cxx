#include "205_isomorphic_strings.hpp"
#include "../test_utils.hpp"
#include <vector>

typedef int TestCase;

void test_isomorphic_strings(TestCase& c) {

}

TEST(leetcode, isomorphic_strings) {
    std::vector<TestCase> cases{};

    for(TestCase& c:cases) {
        test_isomorphic_strings(c);
    }
}
