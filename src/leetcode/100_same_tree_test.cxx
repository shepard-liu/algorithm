#include "100_same_tree.hpp"

#include <vector>

#include "../test_utils.hpp"

typedef int TestCase;

void test_same_tree(TestCase& c) {}

TEST(leetcode, same_tree) {
    std::vector<TestCase> cases{};

    for (TestCase& c : cases) {
        test_same_tree(c);
    }
}
