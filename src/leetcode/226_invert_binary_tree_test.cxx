#include "226_invert_binary_tree.hpp"

#include <vector>

#include "../test_utils.hpp"

typedef int TestCase;

void test_invert_binary_tree(TestCase& c) {}

TEST(leetcode, invert_binary_tree) {
    std::vector<TestCase> cases{};

    for (TestCase& c : cases) {
        test_invert_binary_tree(c);
    }
}
